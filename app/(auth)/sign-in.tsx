import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const Signin = () => {
  return (
    <SafeAreaView className="flex-1">
      <Text className="text-black">Signin Page</Text>
    </SafeAreaView>
  );
};

export default Signin;
