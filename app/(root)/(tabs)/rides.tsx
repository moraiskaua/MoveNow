import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const Rides = () => {
  return (
    <SafeAreaView className="flex-1">
      <Text className="text-black">Rides Page</Text>
    </SafeAreaView>
  );
};

export default Rides;
